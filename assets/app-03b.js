function labelHTML(r){return `<div class="slot"><article class="physical"><img class="label-logo" src="${CONFIG.logo}" alt="KSB"><div class="copy"><p class="field-label">Penerima :</p><div class="recipient-name">${esc(full(r)||'RECIPIENT')}</div><div class="attn">Attn: ${esc(r.attn||'-')}</div><div class="address">${esc(r.address||'-')} ${r.phone?`<i>(${esc(r.phone)})</i>`:''}</div></div><div class="divider"></div><div class="sender"><p class="field-label">Pengirim :</p><div class="sender-name">${esc(r.sender||'KSB INDONESIA')}</div></div></article></div>`}function sheetHTML(rows=labels){const l=LAYOUTS[layout],chunk=(rows.length?rows:[{}]).slice(0,l.n);return `<div class="sheet" style="--cols:${l.c};--rows:${l.r};--cw:${l.w}mm;--ch:${l.h}mm;--scale:${l.s}">${chunk.map(labelHTML).join('')}</div>`}function pagesHTML(rows=labels){const l=LAYOUTS[layout];let out='';for(let i=0;i<Math.max(1,rows.length);i+=l.n)out+=sheetHTML(rows.slice(i,i+l.n));return out}
function fitText(root=document){root.querySelectorAll('.physical').forEach(el=>{const targets=[el.querySelector('.copy'),el.querySelector('.sender'),el.querySelector('.recipient-name')];let s=1;el.style.setProperty('--fit',s);let g=0;while(targets.some(x=>x&&(x.scrollHeight>x.clientHeight+1||x.scrollWidth>x.clientWidth+1))&&s>.52&&g<25){s-=.02;el.style.setProperty('--fit',s.toFixed(2));g++}})}function renderPreview(){$('page').innerHTML=sheetHTML();const l=LAYOUTS[layout];$('statLayout').textContent=l.label;$('statSize').textContent=`${l.w} × ${l.h} mm`;$('layoutCaption').textContent=`${l.label} layout`;$('pageCount').textContent=`${Math.max(1,Math.ceil(labels.length/l.n))} page${labels.length>l.n?'s':''}`;requestAnimationFrame(()=>{fitText($('page'));fitPreview()})}function fitPreview(){const sheet=$('page').querySelector('.sheet');if(!sheet)return;const scale=Math.min(.68,($('stage').clientWidth-28)/sheet.offsetWidth);$('page').style.transform=`scale(${scale})`;$('viewport').style.width=sheet.offsetWidth*scale+'px';$('viewport').style.height=sheet.offsetHeight*scale+'px'}function renderLayouts(){$('layouts').innerHTML=Object.entries(LAYOUTS).map(([k,v])=>`<button class="layout ${k===layout?'active':''}" data-layout="${k}">${v.label}</button>`).join('');document.querySelectorAll('[data-layout]').forEach(b=>b.onclick=()=>{layout=b.dataset.layout;localStorage.setItem('ksb-layout',layout);renderLayouts();renderPreview()})}function renderAll(){renderCards();renderForm();renderLayouts();renderPreview()}
