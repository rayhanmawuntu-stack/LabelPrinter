async function sync(){setStatus('syncing','Connecting to Sheets…');try{const p=await apiGet('ping');if(!p?.success)throw Error('Invalid LabelPrint API response');setStatus('connected',`Sheets connected${p.spreadsheetName?` · ${p.spreadsheetName}`:''}`);const [u,h]=await Promise.all([apiGet('getUsers'),apiGet('getHistory',{limit:500})]);if(u?.users?.length){users=u.users;save('ksb-users',users);renderUsers()}if(h?.history){const map=new Map([...h.history.map(x=>[x.id,{...x,syncState:'synced'}]),...history.map(x=>[x.id,x])]);history=[...map.values()].sort((a,b)=>new Date(b.timestamp)-new Date(a.timestamp));save('ksb-history',history);renderHistory();renderAnalytics()}for(const batch of history.filter(x=>x.syncState==='pending'||x.syncState==='failed'))await syncBatch(batch,false)}catch(e){console.warn('Google Sheets connection failed:',e);setStatus('error','Sheets error · tap for details');window.__lastSheetsError=String(e?.message||e);toast(window.__lastSheetsError)}}async function syncBatch(batch,announce=true){if(!connected){batch.syncState='pending';save('ksb-history',history);return}try{await post('saveBatch',batch);batch.syncState='synced';save('ksb-history',history);if(announce)toast('Saved to Google Sheets')}catch(e){batch.syncState='failed';save('ksb-history',history);if(announce)toast(`Saved locally; sync failed: ${e?.message||e}`)}}
function renderUsers(){$('users').innerHTML=users.map((u,i)=>`<button class="user" data-user="${i}"><div class="user-avatar">${initials(u.name)}</div><b>${esc(u.name)}</b><span>Continue as ${esc(u.nickname||u.name.split(' ')[0])}</span></button>`).join('')+`<button class="user" id="addUser"><div class="user-avatar">＋</div><b>Add profile</b><span>Save to Google Sheets</span></button>`;document.querySelectorAll('[data-user]').forEach(b=>b.onclick=()=>selectUser(+b.dataset.user));$('addUser').onclick=async()=>{const name=prompt('Full name');if(!name)return;const nickname=prompt('Nickname')||name.split(/\s+/)[0];if(!users.some(u=>u.name.toLowerCase()===name.toLowerCase()))users.push({name,nickname});save('ksb-users',users);renderUsers();if(connected)await post('addUser',{name,nickname})}}
function selectUser(i){currentUser=users[i];const nick=currentUser.nickname||currentUser.name.split(' ')[0];$('greeting').textContent=`Hi, ${nick}!`;$('heroName').textContent=nick;$('avatar').textContent=initials(currentUser.name);$('entry').classList.add('hidden');if(connected)post('logLogin',{name:currentUser.name,nickname:nick,source:'github-pages',userAgent:navigator.userAgent}).catch(()=>{})}
function switchView(v){active=v;['create','history','analytics'].forEach(n=>$(n+'View').classList.toggle('hidden',n!==v));document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.view===v));$('subtitle').textContent={create:'Build and review today’s shipping labels.',history:'Review previously generated label batches.',analytics:'Track label usage and recurring recipients.'}[v];if(v==='history')renderHistory();if(v==='analytics')renderAnalytics();if(v==='create')requestAnimationFrame(fitPreview)}
