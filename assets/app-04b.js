function renderAnalytics(){const days=+$('range').value,cut=days?Date.now()-days*86400000:0,data=history.filter(b=>new Date(b.timestamp).getTime()>=cut),total=data.reduce((s,b)=>s+b.labels.length,0),avg=data.length?(total/data.length).toFixed(1):0,companies={};data.flatMap(b=>b.labels).forEach(r=>companies[full(r)]=(companies[full(r)]||0)+1);$('analyticsKpis').innerHTML=[['Generated labels',total,'dark'],['Batches',data.length,'blue'],['Average / batch',avg,'lime'],['Unique recipients',Object.keys(companies).filter(Boolean).length,'']].map(x=>`<article class="metric ${x[2]}"><span>${x[0]}</span><strong>${x[1]}</strong><small>Selected period</small></article>`).join('');const buckets=[];for(let i=6;i>=0;i--){const d=new Date();d.setHours(0,0,0,0);d.setDate(d.getDate()-i);const n=new Date(d);n.setDate(n.getDate()+1);buckets.push({label:d.toLocaleDateString('en-GB',{weekday:'short'}),value:data.filter(b=>new Date(b.timestamp)>=d&&new Date(b.timestamp)<n).reduce((s,b)=>s+b.labels.length,0)})}const max=Math.max(1,...buckets.map(x=>x.value));$('bars').innerHTML=buckets.map(x=>`<div class="bar"><span>${x.value}</span><i style="height:${Math.max(3,x.value/max*100)}%"></i><span>${x.label}</span></div>`).join('');$('ranking').innerHTML=Object.entries(companies).filter(x=>x[0]).sort((a,b)=>b[1]-a[1]).slice(0,7).map((x,i)=>`<div class="rank"><em>${i+1}</em><b>${esc(x[0])}</b><strong>${x[1]}</strong></div>`).join('')||'<div class="empty">No data yet.</div>'}
